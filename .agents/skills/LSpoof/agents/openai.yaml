interface:
  display_name: "LSpoof"
  short_description: "Repo-specific patterns and workflows for LSpoof"
  default_prompt: "Use the LSpoof repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true